package com.naden.namaste.ui.components.models

import com.naden.namaste.models.User

case class User(user: User,
                showTitle: Boolean,
                showSocial: Boolean,
                showMessaging: Boolean,
                value: String)
