package com.naden.namaste.ui.models.cards

case class UserCard(user: UserCard,
                    showTitle: Boolean,
                    showSocial: Boolean,
                    showMessaging: Boolean,
                    value: String)
